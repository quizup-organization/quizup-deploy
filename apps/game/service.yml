apiVersion: v1
kind: Service
metadata:
  name: game
  namespace: quizup-prod
  labels:
    app: game
spec:
  selector:
    app: game
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080

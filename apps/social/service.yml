apiVersion: v1
kind: Service
metadata:
  name: social
  namespace: quizup-prod
  labels:
    app: social
spec:
  selector:
    app: social
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080

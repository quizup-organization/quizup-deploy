apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: quizup-gateway-secret
  namespace: quizup-prod
spec:
  encryptedData:
    QUIZUP_UNUSED_SECRET: CHANGE_ME
  template:
    metadata:
      name: quizup-gateway-secret
      namespace: quizup-prod
    type: Opaque


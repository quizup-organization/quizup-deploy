apiVersion: v1
kind: Service
metadata:
  name: matchmaking
  namespace: quizup-prod
  labels:
    app: matchmaking
spec:
  selector:
    app: matchmaking
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080

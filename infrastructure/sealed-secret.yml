apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: quizup-infra-secret
  namespace: quizup-prod
spec:
  encryptedData:
    POSTGRES_PASSWORD: CHANGE_ME
    RABBITMQ_DEFAULT_PASS: CHANGE_ME
  template:
    metadata:
      name: quizup-infra-secret
      namespace: quizup-prod
    type: Opaque

